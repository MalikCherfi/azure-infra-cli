name: Container Instances PHP

on:
    workflow_dispatch:
        inputs:
            action:
                description: "Action Container Instance PHP"
                required: true
                type: choice
                options:
                    - create
                    - delete

jobs:
    manage-container-php:
        runs-on: ubuntu-latest
        permissions:
            id-token: write
            contents: read

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Login to Azure
              uses: azure/login@v2
              with:
                  client-id: ${{ secrets.AZURE_CLIENT_ID }}
                  tenant-id: ${{ secrets.AZURE_TENANT_ID }}
                  subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

            - name: Deploy PHP Container
              if: github.event.inputs.action == 'create'
              run: |
                  chmod +x container-instances/create-container-instances-php.sh
                  ./container-instances/create-container-instances-php.sh

            - name: Delete PHP Container
              if: github.event.inputs.action == 'delete'
              run: |
                  chmod +x container-instances/delete-container-instances-php.sh
                  ./container-instances/delete-container-instances-php.sh

            - name: Logout
              if: always()
              run: az logout